
;; (add-hook 'python-mode-hook
;;           '(lambda ()
;;              (set (make-local-variable 'ffip-regexp) ".*\\.py")
;;              (whitespace-mode "whitespace")
;;              (setq whitespace-line-column 79)
;;              (setq whitespace-style
;;                    ;'(tabs newline space-mark tab-mark newline-mark lines)
;;                    '(newline lines))))
